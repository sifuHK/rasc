# JSON format configurations require double quotes (") and cannot use single quotes (').
[PROJECT INST]
model_name = sc_model_01
# It will be automatically created if it does not exist.
work_space = ./sc_01_ws
no_cores = -1

[DATA INST]
model_data_file_path = ../../Data/data_01/model_data
oot_data_file_path = ../../Data/data_01/oot_data
performance_data_file_path = ../../Data/data_01/perf_data
train_data_name = Train
y = y
# If there are no weights, you can omit configuring `sample_weight_col`.
sample_weight_col = sample_weight
# If you are not using the group variable count limitation feature, you can omit configuring `X_group_format`.
X_group_format = _g

#Most configurations in [BINS INST] have default values. They can be omitted if using the default values.
[BINS INST]
freqBin_cnt = 20
mono_suggest = True
default_mono=A
default_distr_min=0.02
default_rate_gain_min=0.001
default_bin_cnt_max = 7
default_spec_distr_min=${default_distr_min}
default_spec_comb_policy = A
cust_bins=None

[FILTER INST]
filters = {"big_homogeneity":0.99,"small_iv":0.02,"big_ivCoV":0.3,"big_corr":0.8,"big_psi":0.2}
# Utilize the information from equal-frequency binning to pre-filter features before optimal binning, and skip optimal binning calculations for filtered features. When dealing with a large number of features, it is recommended to configure `fore_filters`.
fore_filters = {"big_homogeneity":0.99,"small_iv":0.02}
# When configuring multiple datasets for `big_homogeneity`, the maximum value is taken as the homogeneity rate for a single variable.
# When multiple datasets are configured for `small_iv`, the minimum value is taken as the IV for a single variable.
# big_ivCoV: For the coefficient of variation of IV, the maximum value calculated between any two datasets is taken as the IV coefficient of variation for a single variable.
# big_psi: The maximum value between any two pairs is taken as the PSI for a single variable.
filter_data_names = {"big_homogeneity":"Train","small_iv":"Train,Valid","big_ivCoV":"Train,Valid,OOT","big_psi":"Train,OOT"}
# Specify variables to be deleted.
user_del = x47_gr9,x88_gr14
# Specify groups to be deleted.
user_del_groups = gr10
# Specify variables to be retained.
user_save = x149_gr24,x118_gr22

# Most configurations in [MODEL INST] have default values. They can be omitted if using the default values.
[MODEL INST]
measure_index=aic
# ${DATA INST:train_data_name} indicates that it holds the same value as train_data_name in [DATA INST].
measure_data_name = ${DATA INST:train_data_name}
pvalue_max=0.01
vif_max=2
corr_max=0.7
default_coef_sign = +
fea_cnt=15
# The number of variables from group gr20 entering the model is limited to a maximum of 2, while the number of variables from group gr24 entering the model is limited to a maximum of 3.
cnt_in_group = {"gr20":2,"gr24":3}
# The number of variables from other groups entering the model is limited to a maximum of 1.
default_cnt_in_group = 1
# If you do not need to set limitations on the number of variables from groups entering the model, you can omit configuring `cnt_in_group` and `default_cnt_in_group`.

[CARD INST]
base_points=500
base_event_rate=0.05
pdo=50

[REPORT INST]
y_stat_group_cols = data_category
score_interval_cnt = 20
show_lift = 5,10,20
score_reverse = True
